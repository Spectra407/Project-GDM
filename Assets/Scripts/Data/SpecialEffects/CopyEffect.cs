using UnityEngine;
//copies effect of previous card
public class CopyEffect : SpecialEffect
{
    public void Execute(CombatManager cm, CardData card)
    {
        //look at second most recent card
        //call DrawEffectManager i guess? but fix it so it takes card  so can use that
        //if this is the first card have it do nothing ig.
    }
}
